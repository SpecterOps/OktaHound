{
  "name": "Okta: Application Credentials",
  "description": "Lists all service application secrets and JWTs in the Okta organization.",
  "query": "MATCH p = (:Okta_Organization)-[:Okta_Contains]->(:Okta_Application)<-[:Okta_SecretOf|Okta_KeyOf]->(:Okta)\nRETURN p\nLIMIT 1000"
}
